services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: aso
      POSTGRES_PASSWORD: aso
      POSTGRES_DB: aso
    ports:
      - "5433:5432"
    volumes:
      - aso_pgdata:/var/lib/postgresql/data

volumes:
  aso_pgdata:
